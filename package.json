{
  "name": "codex-cli-mcp-bridge",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "MCP bridge for invoking Codex CLI from Claude Code (Phase 1: codex_plan, strictly read-only)",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test \"dist/tests/*.test.js\"",
    "start": "node dist/src/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
